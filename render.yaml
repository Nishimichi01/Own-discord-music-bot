services:
  - type: worker
    name: discord-music-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python music_bot.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
